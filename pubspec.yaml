name: ldj_app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter
  provider:
  flutter_gen: ^5.8.0
  flutter_svg: ^2.0.17
  google_fonts: ^6.2.1
  slider_button: ^2.1.0
  slider_button_component: ^1.0.4
  firebase_core: ^3.12.1
  firebase_auth: ^5.5.1
  
  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  flutter_launcher_icons: ^0.14.3
  flutter_native_splash: ^2.4.4

flutter_launcher_icons:
  android: true
  ios: true
  image_path: assets/icons/AppLogoIcon.png

flutter_native_splash:
  image: assets/splash/AppLogoSplash.png

flutter:
  uses-material-design: true
  assets:
  - assets/images/
